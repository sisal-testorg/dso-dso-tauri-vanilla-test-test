name: CI
# Triggers
on:
  workflow_dispatch: # Manual trigger from the Actions tab
jobs:
  ci:
    secrets: inherit
    uses: sisal-testorg/shared-workflows/.github/workflows/betting-workflow.yaml@feature/STSK0160139
    with:
      project-type: "nodejs"
